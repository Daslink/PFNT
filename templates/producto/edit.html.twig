{% extends 'base.html.twig' %}

{% block title %}Editar Producto{% endblock %}

{% block body %}
    <br>
    <center><h1>Editar Producto</h1></center><br>
    <style>
        .crear{
            background-color: #343a40;
            color: rgba(255,255,255,.5);
            height: 50px;
            width: 200px;
        }
    </Style>
    <center>
    {{ include('producto/_form.html.twig', {'button_label': 'Actualizar'}) }}

    </center>

    <center><a href="{{ path('producto_index') }}"><button class="crear">Volver a la lista</button></a></center>

{% endblock %}
