{% extends 'base.html.twig' %}

{% block title %}Producto{% endblock %}

{% block body %}
    <center><h1>Producto</h1></center><br>
    <center>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producto.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ producto.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ producto.descripcion }}</td>
            </tr>
            <tr>
                <th>ImagenURL</th>
                <td>{{ producto.imagenURL }}</td>
            </tr>
            <tr>
                <th>PuntosRedimir</th>
                <td>{{ producto.puntosRedimir }}</td>
            </tr>
        </tbody>
    </table>
    </center>

    <style>
        .crear{
            background-color: #343a40;
            color: rgba(255,255,255,.5);
            height: 50px;
            width: 200px;
        }
    </Style>
    


    {{ include('producto/_delete_form.html.twig') }}
    <center><a href="{{ path('producto_index') }}"><button class="crear">Volver a la lista</button></a></center>

   
{% endblock %}
